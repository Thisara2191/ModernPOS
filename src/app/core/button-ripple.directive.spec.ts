import { ButtonRippleDirective } from './button-ripple.directive';

describe('ButtonRippleDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonRippleDirective();
    expect(directive).toBeTruthy();
  });
});
